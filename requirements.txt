# ============================================================
# AUTOMATED RESUME SCREENING - DEPENDENCIES
# ============================================================
# Install: pip install -r requirements.txt
# ============================================================

# Web Framework
streamlit>=1.28.0

# LLM & Embeddings
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Document Processing
PyMuPDF>=1.23.0           # PDF text extraction & rendering
python-docx>=1.0.0        # DOCX file support
pdf2image>=1.16.0         # PDF to image for OCR
pytesseract>=0.3.10       # OCR text extraction

# HTTP Requests (Ollama + Groq API)
requests>=2.31.0

# Data Processing
pandas>=2.0.0

# Environment Variables
python-dotenv>=1.0.0

# ============================================================
# SYSTEM DEPENDENCIES (install separately)
# ============================================================
#
# macOS:
#   brew install tesseract poppler
#   brew install ollama && ollama pull qwen2.5:3b
#
# Ubuntu/Debian:
#   sudo apt install tesseract-ocr poppler-utils
#   curl -fsSL https://ollama.ai/install.sh | sh
#   ollama pull qwen2.5:3b
#
# ============================================================
# QUICK START
# ============================================================
#
# 1. python -m venv venv && source venv/bin/activate
# 2. pip install -r requirements.txt
# 3. cp .env.example .env  # Edit with your GROQ API key
# 4. ollama serve && ollama pull qwen2.5:3b
# 5. streamlit run app.py
#
# ============================================================